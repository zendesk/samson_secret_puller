source 'https://rubygems.org'

ruby File.read(Bundler.root.join('.ruby-version')).strip

gem 'vault', ">= 0.5.0"

# test
gem 'maxitest'
gem 'mocha'
gem 'webmock'
gem 'single_cov'
gem 'rubocop'
gem 'rack'
gem 'bump'
gem 'rake'
